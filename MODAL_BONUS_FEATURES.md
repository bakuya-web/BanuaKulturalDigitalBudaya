# 🎯 BONUS: Implementation Examples & Custom Snippets\n\n## Contoh Implementasi Tambahan\n\n---\n\n## 1️⃣ Menambah Keyboard Support (ESC untuk close)\n\n**Tambahkan ke dalam function `eduModal.init()`:**\n\n```javascript\ninit: function() {\n  // ... existing code ...\n  \n  // Add keyboard support\n  document.addEventListener('keydown', (e) => {\n    if (e.key === 'Escape') {\n      const modal = document.getElementById('eduModal');\n      if (modal.classList.contains('active')) {\n        this.close();\n      }\n    }\n  });\n}\n```\n\n**Test**: Buka modal → Tekan tombol ESC → Modal tutup ✅\n\n---\n\n## 2️⃣ Tracking Analytics (Google Analytics)\n\n**Tambahkan di function `open()`:**\n\n```javascript\nopen: function(key) {\n  const data = this.data[key];\n  if (!data) return;\n  \n  // Existing code...\n  document.getElementById('modalTitle').textContent = data.title;\n  // ... etc ...\n  \n  // TAMBAH: Analytics tracking\n  if (typeof gtag !== 'undefined') {\n    gtag('event', 'modal_opened', {\n      'material_name': data.title,\n      'material_category': data.badge\n    });\n  }\n  \n  const modal = document.getElementById('eduModal');\n  modal.classList.add('active');\n  document.body.style.overflow = 'hidden';\n}\n```\n\n---\n\n## 3️⃣ \"Download Materi\" Button Functionality\n\n**Update HTML modal-footer:**\n\n```html\n<div class=\"modal-footer\">\n  <button class=\"modal-btn modal-btn-secondary\" id=\"modalBtnDownload\">\n    📥 Download\n  </button>\n  <button class=\"modal-btn modal-btn-secondary\" id=\"modalBtnBack\">\n    Kembali\n  </button>\n  <button class=\"modal-btn modal-btn-primary\" id=\"modalBtnNext\">\n    Lanjut Belajar\n  </button>\n</div>\n```\n\n**Update data (tambah URL file):**\n\n```javascript\n'pengelolaan-keuangan': {\n  title: 'Pengelolaan Keuangan',\n  badge: 'Keuangan Bisnis',\n  icon: '....',\n  description: '....',\n  downloadUrl: '/assets/downloads/pengelolaan-keuangan.pdf' // TAMBAH\n}\n```\n\n**Add event listener di init():**\n\n```javascript\ninit: function() {\n  // ... existing code ...\n  \n  const downloadBtn = document.getElementById('modalBtnDownload');\n  downloadBtn?.addEventListener('click', () => {\n    // Find current material key\n    const title = document.getElementById('modalTitle').textContent;\n    const key = this.getTitleKey(title);\n    const data = this.data[key];\n    \n    if (data?.downloadUrl) {\n      // Trigger download\n      const link = document.createElement('a');\n      link.href = data.downloadUrl;\n      link.download = `${data.title}.pdf`;\n      document.body.appendChild(link);\n      link.click();\n      document.body.removeChild(link);\n      \n      // Analytics\n      if (typeof gtag !== 'undefined') {\n        gtag('event', 'material_downloaded', {\n          'material_name': data.title\n        });\n      }\n    }\n  });\n}\n```\n\n**CSS untuk button (update modal-footer):**\n\n```css\n.modal-footer {\n  padding: 20px 32px;\n  border-top: 1px solid var(--cream-dark);\n  display: flex;\n  gap: 12px;\n  justify-content: flex-end;\n  background: var(--cream);\n}\n\n.modal-btn {\n  /* existing */\n}\n\n/* 3 buttons arrangement */\n@media (min-width: 641px) {\n  .modal-btn-secondary:nth-of-type(1) {\n    margin-right: auto; /* Push download ke kiri */\n  }\n}\n```\n\n---\n\n## 4️⃣ Bookmark / Save Materi\n\n**Add bookmark button ke modal-header:**\n\n```html\n<div class=\"modal-header\">\n  <div class=\"modal-icon\" id=\"modalIcon\"></div>\n  <h2 id=\"modalTitle\" class=\"modal-title\">Judul Materi</h2>\n  <span id=\"modalBadge\" class=\"modal-badge\">Edukasi</span>\n  \n  <!-- BOOKMARK BUTTON -->\n  <button class=\"modal-bookmark\" id=\"modalBtnBookmark\" aria-label=\"Bookmark\">\n    <svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\n      <path d=\"M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z\"></path>\n    </svg>\n  </button>\n</div>\n```\n\n**CSS untuk bookmark button:**\n\n```css\n.modal-bookmark {\n  position: absolute;\n  top: 20px;\n  left: 20px;\n  width: 40px;\n  height: 40px;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  background: var(--cream);\n  border-radius: 50%;\n  color: var(--text-light);\n  border: none;\n  cursor: pointer;\n  transition: all var(--transition);\n}\n\n.modal-bookmark:hover,\n.modal-bookmark.bookmarked {\n  color: var(--accent);\n  background: var(--cream-dark);\n}\n\n.modal-bookmark.bookmarked svg {\n  fill: var(--accent);\n}\n```\n\n**JavaScript functionality:**\n\n```javascript\ninit: function() {\n  // ... existing code ...\n  \n  const bookmarkBtn = document.getElementById('modalBtnBookmark');\n  bookmarkBtn?.addEventListener('click', () => {\n    const title = document.getElementById('modalTitle').textContent;\n    const key = this.getTitleKey(title);\n    const bookmarks = JSON.parse(localStorage.getItem('bookmarkedMaterials') || '[]');\n    \n    const index = bookmarks.indexOf(key);\n    if (index > -1) {\n      bookmarks.splice(index, 1); // Remove\n      bookmarkBtn.classList.remove('bookmarked');\n    } else {\n      bookmarks.push(key); // Add\n      bookmarkBtn.classList.add('bookmarked');\n    }\n    \n    localStorage.setItem('bookmarkedMaterials', JSON.stringify(bookmarks));\n  });\n  \n  // Update bookmark button state when modal opens\n  // (add to open function)\n}\n\nopen: function(key) {\n  // ... existing code ...\n  \n  // Check if bookmarked\n  const bookmarks = JSON.parse(localStorage.getItem('bookmarkedMaterials') || '[]');\n  const bookmarkBtn = document.getElementById('modalBtnBookmark');\n  if (bookmarks.includes(key)) {\n    bookmarkBtn.classList.add('bookmarked');\n  } else {\n    bookmarkBtn.classList.remove('bookmarked');\n  }\n}\n```\n\n---\n\n## 5️⃣ Share Modal Content\n\n**Add share button ke modal-header:**\n\n```html\n<button class=\"modal-share\" id=\"modalBtnShare\" aria-label=\"Share\">\n  <svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\n    <circle cx=\"18\" cy=\"5\" r=\"3\"></circle>\n    <circle cx=\"6\" cy=\"12\" r=\"3\"></circle>\n    <circle cx=\"18\" cy=\"19\" r=\"3\"></circle>\n    <line x1=\"8.59\" y1=\"13.51\" x2=\"15.42\" y2=\"17.49\"></line>\n    <line x1=\"15.41\" y1=\"6.51\" x2=\"8.59\" y2=\"10.49\"></line>\n  </svg>\n</button>\n```\n\n**JavaScript untuk share:**\n\n```javascript\ninit: function() {\n  // ... existing code ...\n  \n  const shareBtn = document.getElementById('modalBtnShare');\n  shareBtn?.addEventListener('click', async () => {\n    const title = document.getElementById('modalTitle').textContent;\n    const url = window.location.href;\n    \n    if (navigator.share) {\n      try {\n        await navigator.share({\n          title: 'Warisan Kalsel - ' + title,\n          text: 'Cek materi edukasi UMKM: ' + title,\n          url: url\n        });\n      } catch (err) {\n        console.log('Share cancelled');\n      }\n    } else {\n      // Fallback: Copy to clipboard\n      const text = `Cek materi edukasi UMKM: ${title}\\n${url}`;\n      navigator.clipboard.writeText(text);\n      alert('Link dicopy ke clipboard!');\n    }\n  });\n}\n```\n\n---\n\n## 6️⃣ Dark Mode Variant\n\n**Add CSS untuk dark mode:**\n\n```css\n/* Dark mode support */\n@media (prefers-color-scheme: dark) {\n  .modal-overlay {\n    background: rgba(0, 0, 0, 0.7);\n  }\n\n  .modal-content {\n    background: #1a1a1a;\n    color: #f0f0f0;\n  }\n\n  .modal-header {\n    border-bottom-color: #333;\n  }\n\n  .modal-title {\n    color: #f0f0f0;\n  }\n\n  .modal-badge {\n    background: #333;\n    color: #ff9800;\n  }\n\n  .modal-body p,\n  .modal-body li {\n    color: #ccc;\n  }\n\n  .modal-list li {\n    background: #2a2a2a;\n  }\n\n  .modal-close {\n    background: #333;\n    color: #f0f0f0;\n  }\n\n  .modal-close:hover {\n    background: #ff9800;\n  }\n\n  .modal-btn-secondary {\n    background: #333;\n    color: #f0f0f0;\n    border-color: #555;\n  }\n\n  .modal-body::-webkit-scrollbar-track {\n    background: #2a2a2a;\n  }\n\n  .modal-body::-webkit-scrollbar-thumb {\n    background: #555;\n  }\n}\n```\n\n---\n\n## 7️⃣ Next/Prev Material Navigation\n\n**Update modal buttons di HTML:**\n\n```html\n<div class=\"modal-footer\">\n  <button class=\"modal-btn modal-btn-secondary\" id=\"modalBtnPrev\">\n    ← Materi Sebelumnya\n  </button>\n  <button class=\"modal-btn modal-btn-secondary\" id=\"modalBtnBack\">\n    Tutup\n  </button>\n  <button class=\"modal-btn modal-btn-primary\" id=\"modalBtnNext\">\n    Materi Berikutnya →\n  </button>\n</div>\n```\n\n**Add data materi keys order:**\n\n```javascript\nconst materiOrder = [\n  'pengantar-umkm',\n  'pengelolaan-keuangan',\n  'pemasaran-digital',\n  'desain-produk',\n  'manajemen-produksi',\n  'legalitas-perizinan'\n];\n```\n\n**Add navigation methods:**\n\n```javascript\ninit: function() {\n  // ... existing code ...\n  \n  const nextBtn = document.getElementById('modalBtnNext');\n  const prevBtn = document.getElementById('modalBtnPrev');\n  \n  nextBtn?.addEventListener('click', () => {\n    const title = document.getElementById('modalTitle').textContent;\n    const key = this.getTitleKey(title);\n    const currentIdx = materiOrder.indexOf(key);\n    \n    if (currentIdx < materiOrder.length - 1) {\n      this.open(materiOrder[currentIdx + 1]);\n    } else {\n      alert('Ini adalah materi terakhir!');\n    }\n  });\n  \n  prevBtn?.addEventListener('click', () => {\n    const title = document.getElementById('modalTitle').textContent;\n    const key = this.getTitleKey(title);\n    const currentIdx = materiOrder.indexOf(key);\n    \n    if (currentIdx > 0) {\n      this.open(materiOrder[currentIdx - 1]);\n    } else {\n      alert('Ini adalah materi pertama!');\n    }\n  });\n}\n```\n\n---\n\n## 8️⃣ Progress Indicator\n\n**Add di modal-header:**\n\n```html\n<div class=\"modal-progress\">\n  <div class=\"modal-progress-bar\" id=\"modalProgressBar\"></div>\n</div>\n```\n\n**CSS:**\n\n```css\n.modal-progress {\n  position: absolute;\n  bottom: 0;\n  left: 0;\n  right: 0;\n  height: 3px;\n  background: var(--cream-dark);\n}\n\n.modal-progress-bar {\n  height: 100%;\n  background: var(--accent);\n  width: 16.67%; /* 1/6 materials */\n  transition: width 0.3s ease;\n}\n```\n\n**JavaScript update progress saat buka materi:**\n\n```javascript\nopen: function(key) {\n  // ... existing code ...\n  \n  // Update progress bar\n  const currentIdx = materiOrder.indexOf(key);\n  const progress = ((currentIdx + 1) / materiOrder.length) * 100;\n  const progressBar = document.getElementById('modalProgressBar');\n  if (progressBar) {\n    progressBar.style.width = progress + '%';\n  }\n}\n```\n\n---\n\n## 9️⃣ Materi Preview di Modal (Image/Video)\n\n**Update data struktur:**\n\n```javascript\n'pengelolaan-keuangan': {\n  title: 'Pengelolaan Keuangan',\n  badge: 'Keuangan Bisnis',\n  icon: '....',\n  image: '/assets/image/edukasi/pengelolaan-keuangan.jpg', // ADD\n  video: 'https://youtube.com/embed/xxxx', // ADD (optional)\n  description: '....',\n}\n```\n\n**Update modal-body HTML:**\n\n```html\n<div class=\"modal-body\" id=\"modalBody\">\n  <div class=\"modal-media\" id=\"modalMedia\"></div>\n  <p id=\"modalDesc\">Deskripsi...</p>\n</div>\n```\n\n**CSS:**\n\n```css\n.modal-media {\n  margin-bottom: 20px;\n  border-radius: var(--radius-sm);\n  overflow: hidden;\n}\n\n.modal-media img {\n  width: 100%;\n  height: 250px;\n  object-fit: cover;\n}\n\n.modal-media iframe {\n  width: 100%;\n  height: 250px;\n  border: none;\n}\n```\n\n**JavaScript update media saat buka:**\n\n```javascript\nopen: function(key) {\n  const data = this.data[key];\n  if (!data) return;\n  \n  // ... existing code ...\n  \n  // Update media\n  const mediaDiv = document.getElementById('modalMedia');\n  if (data.image) {\n    mediaDiv.innerHTML = `<img src=\"${data.image}\" alt=\"${data.title}\">`;\n  } else if (data.video) {\n    mediaDiv.innerHTML = `<iframe src=\"${data.video}\" allowfullscreen></iframe>`;\n  } else {\n    mediaDiv.innerHTML = '';\n  }\n}\n```\n\n---\n\n## 🔟 Loading State untuk Konten Dinamis\n\n**HTML:**\n\n```html\n<div class=\"modal-body\" id=\"modalBody\">\n  <div class=\"modal-loading\" id=\"modalLoading\">\n    <div class=\"spinner\"></div>\n    <p>Memuat materi...</p>\n  </div>\n  <p id=\"modalDesc\" style=\"display: none;\">Deskripsi...</p>\n</div>\n```\n\n**CSS:**\n\n```css\n.modal-loading {\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  padding: 40px;\n  text-align: center;\n}\n\n.spinner {\n  width: 40px;\n  height: 40px;\n  border: 4px solid var(--cream-dark);\n  border-top-color: var(--accent);\n  border-radius: 50%;\n  animation: spin 0.8s linear infinite;\n  margin-bottom: 16px;\n}\n\n@keyframes spin {\n  to { transform: rotate(360deg); }\n}\n```\n\n**JavaScript dengan simulasi loading:**\n\n```javascript\nopen: function(key) {\n  const data = this.data[key];\n  if (!data) return;\n  \n  const modal = document.getElementById('eduModal');\n  const modalLoading = document.getElementById('modalLoading');\n  const modalDesc = document.getElementById('modalDesc');\n  \n  // Show loading\n  modal.classList.add('active');\n  modalLoading.style.display = 'flex';\n  modalDesc.style.display = 'none';\n  document.body.style.overflow = 'hidden';\n  \n  // Simulate content loading (remove after 500ms)\n  setTimeout(() => {\n    document.getElementById('modalTitle').textContent = data.title;\n    document.getElementById('modalBadge').textContent = data.badge;\n    document.getElementById('modalIcon').innerHTML = data.icon;\n    document.getElementById('modalDesc').innerHTML = data.description;\n    \n    modalLoading.style.display = 'none';\n    modalDesc.style.display = 'block';\n  }, 500);\n}\n```\n\n---\n\n## 📋 Implementation Checklist\n\nPilih fitur mana yang ingin ditambahkan:\n\n- [ ] Keyboard support (ESC)\n- [ ] Analytics tracking\n- [ ] Download button\n- [ ] Bookmark feature\n- [ ] Share functionality\n- [ ] Dark mode\n- [ ] Next/Prev navigation\n- [ ] Progress indicator\n- [ ] Media preview\n- [ ] Loading state\n\n---\n\n**Catatan**: Semua contoh di atas bersifat opsional dan dapat dikombinasikan sesuai kebutuhan.\n"